# Geheimen en tokens — NOOIT committen
.env
.token_cache.json

# Opgehaalde bestanden, tekst en rapportage
output/

# Jouw documenten (vertrouwelijk — nooit committen)
input_documenten/*
!input_documenten/.gitkeep

# Python
.venv/
__pycache__/
*.pyc

# Build / install-artefacten
build/
dist/
*.egg-info/
