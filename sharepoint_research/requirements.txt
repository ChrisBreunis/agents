# Inhoudelijke duiding via Claude (jouw eigen ANTHROPIC_API_KEY)
anthropic>=0.69

# Config
python-dotenv>=1.0

# Optioneel: alleen nodig voor de SharePoint/Graph-route (niet voor de lokale flow)
msal>=1.31
requests>=2.32

# Tekst-extractie per bestandstype
python-docx>=1.1      # .docx
pypdf>=5.1            # .pdf
openpyxl>=3.1         # .xlsx
python-pptx>=1.0     # .pptx
striprtf>=0.0.26     # .rtf
